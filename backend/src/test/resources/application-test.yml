spring:
  datasource:
    url: jdbc:h2:mem:neustudy-admin-test;MODE=MySQL;NON_KEYWORDS=USER;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: false
  data:
    redis:
      host: localhost
      port: 6379

app:
  jwt:
    secret: test-jwt-secret
  admin:
    username: admin
    password: admin123456
    nickname: Test Admin
    email: admin@example.com
    phone: 10000000000

ai:
  openai:
    baseUrl: http://localhost
    token: test-token
    model: test-model
